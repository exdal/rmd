[package]
name = "ast"
version = "1.7.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
core = { path = "../core" }
lexer = { path = "../lexer" }
preprocessor = { path = "../preprocessor" }
